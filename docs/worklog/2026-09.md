# Worklog — 2026-09

## 2026-09-19 — Figma bridge bootstrap: token import + workflow docs

What: Established the code-led Figma workflow. Created `docs/figma/GUIDE.md` (rules: code-led, DSDS-is-the-bridge, no orphans, variant sets mirror code prop APIs, everything variable-bound) and `docs/figma/figma-map.json` (sidecar mapping catalog entity IDs → Figma nodes/collections). Wrote `scripts/ds/figma/build-figma-tokens.mjs` (parses `packages/ui/src/styles.css` `:root`/`.dark`, oklch→hex with gamut clamp, rem/px→float) producing `docs/figma/tokens-build.json` (32 semantic, 240 primitive, 8 metric tokens). Pushed all 280 into the "Kernel DS" Figma file (du0qpv9XrTt4HEWdPhesUh) as three collections: Kernel Semantic (Light/Dark modes), Kernel Primitives (scales + light/base/dark viz aliases + status/* lifecycle aliases), Kernel Metrics (radius, control heights, durations, spacing).

Why: Start of a Figma↔DS round-trip workflow where the repo stays source of truth and Figma component work is tracked against the DSDS catalog. The existing "App" shell frame (1:14) is mapped to `object.workspace` as an exploration.

Touched: docs/figma/GUIDE.md, docs/figma/figma-map.json, docs/figma/tokens-build.json, scripts/ds/figma/build-figma-tokens.mjs

Known issue: `figma_export_tokens` reads a stale source for this draft file (sees 0 collections) — pull path uses `figma_get_variables` via the desktop bridge until resolved; `tokens.config.json` deferred.

## 2026-09-19 — Figma: Tokens reference page + first component (Button)

What: Built a "Tokens" page in the Kernel DS Figma file with three reference sheets — Semantic (32 swatches, sheet bg/text bound to semantic vars so mode-switching previews dark), Primitives (19 scale rows, 240 swatches incl. viz light/base/dark and status lifecycle aliases), Metrics (bars width-bound to the FLOAT variables). Built the first component: Button component set (node 1:1299, key f0357ede…) on a "Components" page — 24 variants, Variant (default/outline/secondary/ghost/destructive/link) × Size (default/xs/sm/lg), mirroring `buttonVariants` in packages/ui/src/components/ui/button.tsx. Heights bound to control-h/-sm/-lg metrics variables, all fills/strokes/text bound to semantic variables, destructive at 10% fill opacity matching `bg-destructive/10`. Icon sizes omitted for now.

Why: Tokens page gives designers a live, variable-bound reference (edits to variables propagate to the sheets). Button establishes the component-set-mirrors-code-API convention from GUIDE.md.

Touched: docs/figma/figma-map.json, docs/figma/GUIDE.md (Figma-side: Tokens page 1:357, Components page 1:1250, Button set 1:1299)

Addendum (same day): added 18 Figma text styles mirroring the code type scale — text/{2xs,xs,sm,base,lg}/{regular,medium} + text/{xl,2xl,3xl}/{regular,semibold} + mono/{sm,xs}; Inter stands in for the system-ui sans stack, Roboto Mono for mono; sizes are Tailwind defaults plus the custom text-2xs (11px/16px) step from styles.css. Built a Typography reference section on the Tokens page (section 1:1485) and re-applied Button labels to text/sm/medium (text/xs/medium for Size=xs); sm labels later set to 12.8px matching code's text-[0.8rem].

Addendum 2 (same day): filled out the underlying Tailwind-layer metrics as Figma variables in Kernel Metrics (+48, now 64 FLOATs): radius/{sm,md,lg,xl,full} (0/2/4/8/9999 — shadcn calc off --radius 4px), spacing/{0,px,0-5..24} at the Kernel-rebased unit (--spacing 0.24rem → spacing/1 = 3.84px; fractional px values are faithful to rendered output, kept exact deliberately), border/{1,2,4}, fontSize/{2xs..4xl}, lineHeight/{2xs..4xl}. All values are px — rem lives in code only; conversion (rem×16) happens in the import parser. Rebound Button to them: corner radii → radius/lg (radius/md for xs/sm per rounded-[min(var(--radius-md),…)]), paddingL/R → spacing/{3,2,2-5,3-5}, itemSpacing → spacing/{1-5,1}; re-applied destructive 10% fill opacity after the arranged container was undone.

## 2026-09-19 — Spacing unit → 0.25rem (decision 0064)

What: Changed `--spacing` from 0.24rem to 0.25rem in packages/ui/src/styles.css — spacing utilities now land on the whole-pixel 4px grid. Updated the 21 Figma spacing variables (Kernel Metrics) to match (spacing/1 = 4 … spacing/24 = 96, base spacing = 4) and regenerated docs/figma/tokens-build.json.

Why: The 0.24rem rebase made every rendered spacing fractional (3.84px grid), which fought the Figma variable workflow and bought nothing design-wise. Surfaced during Figma token import.

Touched: packages/ui/src/styles.css, docs/decisions/0064-spacing-unit-quarter-rem.md, docs/figma/tokens-build.json. Verified: kernel-portal `npm run build` passes. Follow-up: visual audits (mobile/contrast) on next kernel-verify pass.

## 2026-09-19 - Verify pass after spacing change; contrast-audit path fix

What: Ran the follow-up audits for decision 0064. ds:doctor 9 checks/0 violations; portal build clean; contrast audit 70 pairs, 0 below AA 4.5:1; mobile audit 0/0/0/0 on /, /components/button, /patterns (served dist via vite preview :4600). Fixed contrast-audit.mjs still reading kernel-portal/src/index.css - now reads packages/ui/src/styles.css (tokens moved there).

Why: Spacing change made the portal ~4% roomier; audits confirm no regressions. The stale CSS path made the contrast gate error out entirely.

Touched: kernel-portal/scripts/contrast-audit.mjs, docs/worklog/2026-09.md

## 2026-09-19 - Figma Input component set

What: Built the Input component set in Figma (set 5:45, Components page) with a State axis (default/focus/invalid/disabled) mirroring the code component pseudo-class styling - the code Input has no cva variants. All properties variable-bound: height control-h, radius radius/lg, padding spacing/2-5 + spacing/1, borders semantic/input|ring|destructive, placeholder text/sm/regular on semantic/muted-foreground. Focus/invalid rings are 3px-spread drop shadows; binding a shadow color to a variable forces alpha to 1, so added two alpha-baked semantic variables (semantic/ring-50, semantic/destructive-20 with dark /40) matching code ring-ring/50 and ring-destructive/20 dark:/40.

Why: Second component after Button; states are the part Figma consumers actually need for form design.

Touched: docs/figma/figma-map.json (Figma-side: set 5:45, vars 5:68/5:69)

Addendum (same day): elevation tokens imported as Figma effect styles - elevation/{light,dark}/{sm,md,lg,xl,2xl} (10 styles) from the styles.css shadow ramp. Figma has no shadow variable type and effect styles cannot mode-switch, so light/dark are separate trees; 2xs/xs skipped (transparent by design); --shadow duplicates --shadow-sm so no separate style. Shadow color oklch(0.16 0.022 165) light / oklch(0.04 0.018 165) dark converted via culori; alphas 4-14% light, 28-55% dark. Elevation reference section added to Tokens page (5:80).

Addendum: foundations audit vs styles.css (307 custom props) - all covered: 34 semantic, 240 primitives (batch-1 warning/success order anomaly confirmed benign, all present), 59 metrics, 10 elevation effect styles, 18 text styles. Gap closed: ease/{out,in-out,spring} added as STRING vars in Kernel Metrics (Figma has no easing type; documentation-only). letter-spacing/tracking-normal are 0em (nothing to represent); shadow-* component props baked into effect styles.

Addendum: Layout section added to Tokens page (6:118) - documents the grid system: 8px base grid, 24 columns (desktop 1728x1134), 8px gutter, 8px page margin, 8px row rhythm; all bound to spacing/2 on the App frames. Includes 24-column diagram. Second App frame (4:7) observed with the same token-bound grids as 1:14.

Addendum: Sidebar built in Figma with proper nesting - atoms: Sidebar / Menu Button set (7:215, 18 variants Variant x Size x State, mirrors sidebarMenuButtonVariants cva: default/outline x default 32/sm 28/lg 48 x default/hover/active), Sidebar / Group Label (7:216), Sidebar / Separator (7:218); assembly: Sidebar component (7:220, 256x640 = 16rem width) composed entirely of instances - header (lg button), Workspace group (Dashboard active + 3 items), separator, System group, footer pinned. All fills/radii/spacing/padding bound to semantic/sidebar-* + radius/md + spacing/2; labels use text/sm + text/xs styles. Bind-drops-opacity quirk hit again (icon 0.9, group-label 0.7) and fixed via post-set.

Addendum: Components-page organization rules added to docs/figma/GUIDE.md (one section per catalog entity, vertical stack, namespaced sub-components, sections as undo firewall, figma-map.json as recovery register). Sidebar family wrapped in section 7:289; page has zero loose nodes. DISCOVERED: Button set (1:1299) and Input set (5:45) were deleted from the file (likely undo collateral) - need rebuild from code API + map entries.

Addendum: Button + Input rebuilt after file-side deletion. New IDs: Button set 7:449 (24 variants, section 7:399), Input set 7:347 (4 states, section 7:400), Sidebar section 7:289. First Button rebuild (7:338) vanished during post-build section reparenting; second rebuild created the set directly inside its section and survived. Rule reinforced in GUIDE: build inside the section from the start. Components page: 3 sections, zero loose nodes.

Addendum: installed southleft/figma-console-mcp-skills (23 skills) into .agents/skills via npx skills add; ran agents:generate + skills:check (both green). Applied the skill-pack best practices to all 333 Figma variables: scopes set (semantic+primitives -> fills/stroke/effect color; radius -> CORNER_RADIUS; spacing -> GAP+WIDTH_HEIGHT; control-h -> WIDTH_HEIGHT; border -> STROKE_FLOAT; fontSize/lineHeight -> typography scopes) and WEB code syntax on 307 vars matching the CSS custom property names from styles.css (--primary, --color-brand-500, --radius-lg, --text-sm...). Verified via read-back sample.

Addendum: validated light/dark switching via explicitVariableModes (Button dark preview, instances-in-frame pattern). Root-caused recurring destructive-opacity bug: paint opacity + bound color is reset to 1 by Figma on bind/clone/instance/mode-reeval. Fixed permanently with alpha-baked variable semantic/destructive-10 (light 10%, dark 20% per code) bound at paint opacity 1. GUIDE.md hardened-gotchas section added. Dark preview frame 10:500 kept in Button section.

Addendum: promoted figma-map.json to a validated DSDS registry (Tier 1). New scripts/ds/figma/check-figma-map.mjs — validates every mapped entity ID against the catalog, node-id/key/kind/status shape per entry, plus a figmaCoverage() report. Wired as doctor check "figma-map" (ds:doctor now 10 checks). Red-tested with a bogus entity (4 violations caught: unknown-entity, missing-node, invalid-kind, invalid-status), green after revert. Recovery note: the red-test git checkout reverted figma-map.json to commit e3deb12, losing post-commit entries — rebuilt the map from live Figma truth (all IDs verified against the file). Tier 2 (optional figma field on CatalogEntity via decision record) deferred until the workflow proves out.

Addendum: added npm run ds:figma — standalone figma-map gate + coverage report (FIGMA-COVERAGE component 3/62, element 0/6, pattern 0/10, domain 0/2, object 1/15; FIGMA-MAP-OK). AGENTS inventories regenerated; ds:doctor green (10 checks).

Addendum: real MDI icons in the Figma Sidebar. Refreshed FIGMA_ACCESS_TOKEN unlocked REST library search of the published Material Design Icons library (KuMFRr3zRvRlkNpHaHZnce, 5129 components). Menu Button set gained an Icon INSTANCE_SWAP property (Icon#14:0, default view-dashboard-outline) with a 16px nested icon instance per variant, colored like the label; Sidebar rows swapped to matching icons (barley switcher, contracts/deliveries/settlements/reports/settings, account footer). Gotcha logged: importComponentByKeyAsync hangs silently until the library is enabled in the file's Assets panel.

Addendum: Button set gained the State axis (default/hover/focus/disabled) — 96 variants mirroring the cva's interaction styling. New semantic vars primary-80 (alpha), secondary-hover (premixed color-mix), destructive-hover (alpha 20/30). Focus = ring border + ring-50 3px spread shadow with showShadowBehindNode:false (new gotcha: spread shadows bleed through translucent fills). Disabled = node opacity 0.5. GUIDE gained the live-probe ritual (bridge-side getNodeByIdAsync walk of figma-map at session start) answering the static-gate blindness to Figma-side deletions; probe run clean today (7/7 nodes alive).

Addendum: App shell theme-switching complete. Root-caused why explicit Light mode rendered dark: shell surfaces had been hand-bound in the Figma UI to neutral/900-950 primitives (single-mode, mode-immune); the fills my hex scan matched were visible:false decoys. Rebound all visible painters to semantic/background, card, sidebar-accent on both App frames; 1:14 pinned Dark, 4:7 pinned Light as a validation pair. Two gotchas logged: primitives-on-surfaces kill theme switching, and identical-byteLength screenshots are cached exports (bust with a scale change).

Addendum: dark --background deepened to oklch(0.165 0.020 163) (#06110C, = neutral-950) at user direction - page bg now darker than card/sidebar surfaces for clearer surface hierarchy. Change made code-first in styles.css, then synced to the Figma semantic/background Dark mode value; 4:7 App frame fill rebound from neutral/950 primitive back to semantic/background per the no-primitives-on-surfaces rule. Contrast audit 70 pairs 0 failures; portal build clean; tokens-build.json regenerated.

## 2026-09-20 — Post-merge v2 token re-sync to Figma

What: Merged the current mainline v2 token direction into the Figma bridge. Extended `build-figma-tokens.mjs` to recursively resolve CSS `var()` aliases, preserve semantic alias metadata, parse the lime scale, and evaluate `panel-radius`/`panel-inset` calc recipes. Synced Figma semantic Light/Dark modes using real aliases to primitive variables, added 14 lime variables and two panel metrics, refreshed six alpha/mix derivative variables, and added the lime scale row to the Primitives reference sheet. Figma now has 353 variables: Semantic 38, Primitives 254, Metrics 61.

Why: Main changed dark surfaces to the v2 neutral elevation model and dark actions to lime. The prior Figma state still represented the superseded green dark theme.

Touched: scripts/ds/figma/build-figma-tokens.mjs, docs/figma/tokens-build.json, docs/figma/GUIDE.md, docs/figma/figma-map.json, docs/worklog/2026-09.md. Verified with live read-back plus screenshots of the primitive sheet, dark App shell, and dark Button instances.

## 2026-09-20 — Prototype registry governance contract

What: Added decision 0070 and the first version of the DSDS-linked prototype registry contract. Figma and `kernel-app/` are peer discovery surfaces; canonical packages remain shipped authority. Added a versioned registry schema, pure semantic validator, complete transition authorizer, committed-acceptance and immutable-canonical-evidence resolvers, an empty canonical registry seed, and the `com.kernel.prototype` DSDS extension.

Why: The Figma map and prose drift register could not represent concern-level workflow/data discoveries or prove when accepted prototype work became canonical. Real reusable contracts need a guarded path to `@kernel/definitions` without making sample data or prototype runtime behavior authoritative.

Touched: `docs/decisions/0070-prototype-discovery-and-promotion-authority.md`, `docs/prototypes/registry.json`, `scripts/ds/prototype/`, `scripts/ds/lib/dsds-contract.mjs`, DSDS fixtures/output, `docs/STATE.md`, and this worklog.

## 2026-09-20 — Deterministic prototype lifecycle commands

What: Added the public `ds:prototype` command surface for checking, reporting, creating, linking, transitioning, and projecting prototype initiatives. Mutations validate before writing, use atomic replacement, preserve no-op bytes, and route state changes through the Phase 1 transition authorizer. Doctor now checks registry validity and generated status freshness; status reports pending calls, promotion readiness, canonical owners, and Figma/`kernel-app` coverage by catalog kind.

Why: Concern-level prototype state needs an agent-operable lifecycle rather than hand-edited JSON. The command records discoveries from both peer surfaces while refusing illegal or weakly evidenced promotion without changing the registry.

Touched: `package.json`, `scripts/ds/cli.mjs`, `scripts/ds/commands/{prototype,doctor}.mjs`, `scripts/ds/prototype/{store,projection}.mjs`, prototype CLI tests, `scripts/ds/__check__.mjs`, generated AGENTS inventories, `docs/prototypes/{README,status.md}`, `docs/STATE.md`, and this worklog. The uncommitted proof harness records identical before/after hashes for a refused direct promotion.

## 2026-09-20 — Unified prototype operational state

What: Migrated the hand-authored Figma recovery map and the v2 prototype drift queue into the typed prototype registry. Added a checked manifest covering 91 Figma field paths and 79 identifiable drift entries, seeded seven initiatives with 11 concern records spanning live Figma nodes and `kernel-app` routes, and preserved legacy provenance through schema-distinct `migration-import` events. `ds:generate` now writes both the byte-compatible Figma map and the human-readable prototype status projection; `ds:prototype`, `ds:doctor`, and `ds:figma` share the same validation path.

Why: Figma metadata, executable-prototype discoveries, and historical drift could not remain separate hand-maintained authorities. One registry now owns operational state without flattening the detailed rationale in `docs/v2-prototype-drift.md` or treating a built prototype artifact as automatically promoted.

Touched: `docs/prototypes/{registry.json,migration-manifest.json,status.md,README.md}`, generated `docs/figma/figma-map.json`, `docs/figma/GUIDE.md`, `docs/v2-prototype-drift.md`, prototype migration/projection modules and tests, DS generation/doctor/Figma checks, `docs/STATE.md`, and this worklog. The first generated Figma projection is byte-identical to the pre-migration compatibility fixture.

## 2026-09-20 — Conversational prototype and promotion protocol

What: Added the repository-owned `kernel-prototype` skill as the default ritual for Figma, `kernel-app`, real contract-data/workflow, prototype-only, and promotion requests. Existing implementation and verification skills now delegate concern-level bookkeeping to that single protocol. Skill integrity and cold-agent fixtures cover six representative conversations, including the no-acceptance stop and the required three-commit promotion sequence; the public proof harness now records an ordinary data concern in the same conversational flow.

Why: The registry and lifecycle CLI are agent infrastructure, not forms the user should maintain. A cold agent must infer the correct catalog entity, concern, owner, validation surface, and promotion stop from ordinary direction without silently making Figma or the executable prototype canonical.

Touched: `.agents/skills/kernel-prototype/SKILL.md`, routing guidance in Kernel implementation/verification skills and `docs/figma/GUIDE.md`, skill integrity and conversational scenario fixtures/tests, generated AGENTS inventories, the uncommitted proof harness, `docs/STATE.md`, and this worklog.

## 2026-09-20 — Complete prototype promotion proof

What: Extended the existing public-CLI proof through a complete Figma plus executable-prototype real-data lifecycle. The deterministic harness now verifies a pre-existing `object.workspace` / `compositionContract` implementation, refuses direct and missing-evidence promotion without mutation, rejects a removed or changed acceptance artifact at transition time, and proves the normal three-commit sequence for a newly implemented contract-data export before generating the Figma/status projections and reporting both concerns promoted to `@kernel/definitions`.

Why: Promotion must be demonstrably tied to committed canonical facts and tuple-specific human acceptance rather than UI completeness, sample data, or an arbitrary source file. The proof exposed and fixed a transition-time gap where a committed acceptance removed from the working tree could still be recorded as promoted.

Touched: `scripts/ds/prototype/evidence.mjs`, `scripts/ds/commands/prototype.mjs`, prototype contract tests, `docs/prototypes/README.md`, `docs/STATE.md`, this worklog, and the uncommitted proof harness/transcripts. Two clean runs produce byte-identical transcript hashes.

## 2026-09-20 — Hardened prototype promotion at ship review

What: Closed the adversarial ship-review gaps in the prototype lifecycle. Persisted histories now enforce the transition matrix, migrated concerns retain immutable genesis provenance while advancing, promoted evidence must belong to an initiative entity and to the historical package containing its canonical source, legacy promoted imports require verifiable prior acceptance/canonical evidence without a gap, and both public and generated projection paths refuse invalid or unavailable promotion evidence before writing.

Why: A hand-edited registry, unrelated canonical contract, stale migration marker, cross-package source path, or weak projection path must never present prototype-only work as canonical. The shared validation gate and regression fixtures make those claims independently checkable across transition, check, doctor, project, and generation paths.

Touched: prototype validator/evidence/shared-validation/projection command modules, strict historical catalog parsing, lifecycle contract tests, `scripts/ds/AGENTS.md`, `docs/STATE.md`, and this worklog. The final adversarial re-review reported no remaining must-fix findings.

## 2026-09-21 — Keep the acceptance path present in clean checkouts

What: Added a tracked README under `docs/prototypes/acceptances/` so the skill-integrity gate can resolve the documented acceptance-artifact path in a clean CI checkout.

Why: Git does not retain empty directories. Local verification passed only because the untracked empty directory existed, while the automation job correctly failed `skills:check` after checkout.

Touched: `docs/prototypes/acceptances/README.md` and this worklog.

## 2026-09-22 — Explain DSDS reconciliation to both agents

What: Added a shared operating brief for the Figma agent and design-system repository agent. It defines the three surfaces, catalog/DSDS identity join keys, canonical ownership, separate agent responsibilities, a read-only reconciliation report, a safe one-entity vertical slice, refusal rules, and the evidence required in agent-to-agent handoffs.

Why: The work product design already uses DSDS while the Kernel DS Figma library remains a partial first draft. Both agents need one concise model for reconciling the library, product design, and work repository without treating the newest surface as automatically authoritative or attempting unsafe two-way overwrite.

Touched: `docs/figma/DSDS-AGENT-HANDOFF.md`, `docs/figma/GUIDE.md`, `docs/prototypes/README.md`, `docs/AGENTS.md`, `docs/STATE.md`, and this worklog.
