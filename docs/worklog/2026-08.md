# August 2026

## 2026-08-01 — Expose Sidebar as a component page

Added the ready `@kernel/ui` Sidebar to the Navigation gallery registry so `/components/sidebar` now appears in the Components rail and index and renders the existing Sidebar documentation. The preview composes public Sidebar primitives inside a bounded inner provider, preserving the outer portal shell during collapse and mobile-sheet interactions. Updated the catalog anchor to `c-sidebar` and added the Sidebar route to the existing legacy-route proof manifest.

Verification covered catalog integrity, component-doc parity and coverage, prose/schema gates, portal build and lint, CSS and boot smoke gates, DS doctor/changed-path checks, and desktop/mobile browser interaction proof.

**Touched:** `kernel-portal/src/components/portal/gallery-nav.tsx`, `kernel-portal/src/components/portal/sidebar-cluster.tsx`, `kernel-portal/scripts/legacy-routes-manifest.mjs`, `packages/catalog/src/entities.ts`, `docs/STATE.md`, `docs/worklog/2026-08.md`.
