# Worklog — 2026-08

Append-only. Newest entry at the bottom. Format: see `docs/GUIDE.md`.

## 2026-08-01 — Kernel-styled Sonner toasts

**What:** Replaced the shared Sonner wrapper's undefined `cn-toast` hook with token-backed class mappings for the toast surface, description, action button, cancel button, shadow, and keyboard focus ring.

**Why:** Sonner injects its own unlayered component CSS at runtime, so ordinary shadcn utility classes did not override its default chrome. Important Kernel utilities keep the override colocated with the wrapper and make every portal toast inherit the design system.

**Touched:** `packages/ui/src/components/ui/sonner.tsx`, `docs/STATE.md`, `docs/worklog/2026-08.md`.

## 2026-08-02 (1) — The dev server was blank, and it was a browser release

**What:** `ScrollTop` in `kernel-app/src/components/shell.tsx` was
`React.useEffect(() => window.scrollTo(0, 0), [pathname])`. In Chrome 151
`window.scrollTo` returns a scroll-completion Promise, so the concise arrow
body handed that Promise to React as the effect's *cleanup function*. React
threw `TypeError: destroy is not a function`, unmounted the tree, and the app
painted nothing but the dark background. Fixed with a block body.

**Why it is worth recording.** The line was correct when written; a browser
release turned it into a crash. And it beat every gate we have — `tsc` passes
(the types are fine), `vite build` passes (the bundle is fine), and Part 7's
audits never drive a route change in a current browser. StrictMode
double-invokes effects, so dev crashed on *mount*, which is the only reason it
was loud; the production build survives mount and would have thrown on the
first navigation instead. The deploy preview for PR #72 was almost certainly
broken the same way for anyone on a current Chrome.

**The debugging mistake.** I twice reasoned "`scrollTo` returns undefined, so
this line is fine" and looked elsewhere — at `startViewTransition`, then at a
suspected duplicate `react-router-dom` instance. Both were dead ends, and the
router one was self-inflicted: my console probes loaded modules at unhashed
URLs, creating separate module instances and a fake
"useLocation() outside a Router" error. What actually settled it was evaluating
`String(window.scrollTo(0, 0))` in the running page: `[object Promise]`.
Checking the browser rather than reasoning about the spec.

Audited the rest of the repo for the same shape: the portal's `ScrollManager`
(`kernel-portal/src/pages/portal-layout.tsx`) and the two other `scrollTo` call
sites all use block bodies. Prototype-local, so Part 4 gains nothing.

**Verified:** dev renders with zero console errors; `/`, `/scenarios`,
`/producers` navigate clean (the crash only fired on route change); `tsc` +
`vite build` clean. Temporary console-capture hook added to `kernel-app/
index.html` during debugging and removed — `git status` shows only the
intended files.

**Touched:** kernel-app/src/components/shell.tsx, docs/v2-prototype-drift.md
(5.10, Part 7), this worklog.

## 2026-08-02 (2) — The prototype is the forward track, and the register is a queue

**What:** Decision **0056**, amending 0040. `kernel-app/` is no longer framed
as a sandbox to be abandoned cleanly; it is where the next version of the
design system gets designed, and `docs/v2-prototype-drift.md` is the queue for
promoting that work into the parent DS. Reframed the register's thesis
sections to match: the lead, the summary table, Part 5's opening, and Part 8.

**Why:** 0040's operative rule was "fixes flow upstream, styling does not."
That was right for a bug hunt and wrong for what this became. Under it,
everything the prototype learned about *the system* — the inverted dark
elevation model, the 3.5× radius, the panel header language — got filed as
"styling," which meant "never leaves." The register recorded 27 remapped
tokens and 13 rule groups and then labelled all of them "no — that *is* the
look." So the prototype was allowed to discover design direction but forbidden
from returning any of it, which is not what the work has actually been.

Each entry now carries a promotion status (`promote` / `prototype-only` /
`undecided`) instead of a merge-worthiness verdict, and "undecided" is
explicitly a legitimate resting state. The three-layer discipline picks up a
second justification: it was about reversibility, and it is now also what makes
drift *consumable*, since a token override already names a role token and a
modification-layer rule already names a `data-slot` part. Promotion is mostly
moving an entry down a layer.

**Two things found while doing it.** The summary table said Part 5 held 6
items; it holds 9 (now 10 with 5.10) — it had gone stale as 5.7–5.9 were
appended. Corrected. And this branch carries **three duplicate decision
numbers**: 0040, 0041 and 0042 each exist twice, once from the prototype and
once from main, coexisting since the merge in f87d311. Numbering is supposed to
be a sequence, so this is a real collision, not cosmetic. The new record is
0056 rather than 0053 because the other worktree
(`feat/dsds-contract-tooling`) has already minted through 0055 — avoiding a
fourth collision rather than fixing the first three, which needs its own call.

**Cost, recorded deliberately:** the prototype stops being disposable. Its
worst case used to be a clean delete; the register is now a dependency of the
DS roadmap and goes stale if drift lands unrecorded.

**Verified:** all six reframe anchors matched uniquely (the edit script fails
loudly on a missing or ambiguous anchor rather than writing a partial file);
em-dash and CRLF encoding intact after rewrite; app `tsc` + `vite build` clean;
dev server still renders.

**Touched:** docs/decisions/0056-prototype-is-the-forward-design-track.md
(new), docs/v2-prototype-drift.md, docs/STATE.md, this worklog.

## 2026-08-02 (2) — The expanded row becomes a lit panel in a well

**What:** The Scenarios detail row was a flat arrangement on a shaded strip:
figures on the strip, tabs under them, a framed table below. It is now a recess
holding one lit plate. The well (`[data-v2-detail]`) takes a real fill two rungs
below the darkest zebra stripe and a four-edge inset shadow; the panel
(`[data-v2-panel]`) carries the roll-up figures, the range tabs and the activity
table on a single `--card` surface with a lip and a cast. Tables everywhere now
stripe by default, the open parent row takes the DS's `selected` state, and the
panel's table is back on the app's top-level padding step with air above and
below it.

**Why the panel casts a shadow.** 3.15 says a frame nested on a card takes the
lip and a fill but no cast — a shadow at both levels reads as upholstery. This
one breaks that rule on purpose, and the register says so. 3.15 governs a plate
on a plate, where the surround is already lifted; here the surround is a
*recess*, and a plate in a recess with no cast reads as painted on the floor.
The rule is right and the exception is the case it does not cover.

**The measurement that changed a decision twice.** The first well carved with a
`--foreground` mix — a *white* glow in a recess in dark mode, which is not what
a recess does. Mixing toward black in dark and toward foreground in light fixed
it. Then muted text on the light well measured 4.09:1, under AA: `--muted-foreground`
on `--neutral-200` is simply too close. Resolving it one rung darker inside the
well takes it to 5.65:1. Both were caught by measuring in the running page, not
by looking at the colours.

**Bids are basis everywhere.** Producers quoted basis; Scenarios and the
Overview book quoted cash prices for the same quantity. The scenario seed data
is now basis and one formatter serves all three screens.

**Verified:** `npm run build` green (975 modules, 615ms; CSS 222.79 kB / gzip
33.45). Measured in the running page, dark then light: panel RGB 31,41,36 /
255,255,255 against a well of 6,17,12 / 227,230,228; panel title 14.34:1 /
17.33:1, subtitle and table header 5.79:1 / 7.11:1, timestamp cell 4.97:1 /
6.40:1, cell on a stripe 14.34:1 / 17.33:1 — all clear of AA. Selected row fill
resolves to 9% foreground and now outranks the 5% stripe under it. Panel padding
measured 15.36px top/bottom on the header, 23.04px at the edges, 11.52/15.36px
above and below the table, 38.4px table header.

**Touched:** `kernel-app/src/index.css`, `kernel-app/src/v2-layer.css`,
`kernel-app/src/pages/scenarios.tsx`, `kernel-app/src/pages/producers.tsx`,
`kernel-app/src/pages/overview.tsx`, `kernel-app/src/data/scenarios.ts`,
`kernel-app/src/lib/format.ts` (new), `docs/v2-prototype-drift.md`.

**Still open:** `OfferInset` on Producers is the same object built twice (5.5)
and still has the old flat treatment — it gets the stripes and the selected row
for free, but not the panel.

## 2026-08-03 — The register catches up with the panel, and two numbering
collisions get resolved

**What:** brought `docs/v2-prototype-drift.md` level with the code after a long
run of design work that had been committed but not written down. Eight new rule
groups in Part 3 (3.24–3.31), four new app-level entries in Part 5 (5.14–5.17),
a new 2.4 collecting the tokens the prototype *invented* rather than re-pointed,
and Part 7/8 updated to match. Also rewrote the marker inventory in
`v2-layer.css`'s own head comment, which still described three markers when the
file had eighteen.

**Why:** the register is the promotion pipeline (decision 0056), so a register
that trails the pixels is not documentation debt — it is the mechanism not
working. Everything from the segmented control through the Accept dialog was in
commits and nowhere else, which meant nothing in that range could be argued
about for promotion.

**Two collisions, both from working on two tracks at once:**

- The merge that reconciled this branch with origin had both sides claiming row
  **3.18** — but the row was the *same rule* on both sides, renumbered on the
  remote to make room for the trough. One duplicate dropped, and the trough took
  the next free number (3.23) rather than the inset rows shifting up: 3.21 cites
  3.18 by number, and a register whose numbers move under their own citations is
  not a register.
- Part 5 had **two 5.10s and two 5.11s** for the same reason — each track minted
  the next two numbers while apart. The published side kept its numbers (its
  citations are already pushed); the local pair moved to 5.12 and 5.13. This is
  the third time this repo has hit collision-by-parallel-track, after decisions
  0040–0042.

Part 5's entries had also drifted out of numeric order as both tracks appended;
they are now sorted, since a register read by number should be readable in
number order.

**Verified:** app build green. Marker audit script cross-checks every
`data-v2-*` in the CSS against the register — was eight missing, now zero, with
`data-v2-flag` and `data-v2-meter` explicitly recorded as hooks that carry no
rule rather than left looking like omissions. Every `kernel-app/src/…` path the
register cites still exists.

**Touched:** `docs/v2-prototype-drift.md`, `docs/STATE.md`,
`kernel-app/src/v2-layer.css` (head comment only).

**Still open:** the four roll-up cards each redraw the same axis and `Rejected:
1` charts as a lone spike — recorded as an open correction in 5.15 rather than
fixed. `OfferInset` on Producers is still the same object built twice (5.5) with
the old flat treatment. The Accept dialog's *value over top comp* disagrees with
the reference frames and the register now says which one the code follows and
why.
