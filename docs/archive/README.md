# Archive

Cold storage for content retired from `STATE.md` (completed initiatives,
retired surfaces, resolved question clusters worth keeping) and any
superseded standalone docs.

- File naming: `YYYY-MM-DD-topic.md`
- Each file starts with one line saying why it was archived and, where
  relevant, which worklog entries cover the work.
- Files here are read-only history — never edited after landing.

