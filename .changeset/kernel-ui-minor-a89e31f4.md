---
"@kernel/ui": minor
---

The v2 token direction lands: a full lime scale (300/500 pinned to the shipped accent literals) with every lime role token repointed at it, dark's elevation model inverted (cards raised on a neutral-900 canvas, roles derived from the DS scales), light's rail dropped to neutral-100 with neutral nav selection, radius 0.25rem to 0.875rem, and the elevation-collection tokens (--elev-lip/cast/plate/edge-page) in both themes.

<!-- kernel-ds:release-meta
{"classification":"runtime","breaking":false,"scope":"package"}
-->
