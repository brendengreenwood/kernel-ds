---
name: kernel-ds-bogus
description: Red fixture — references a removed command path, an unknown npm script, and a missing entity, with no verification section.
---

# Bogus skill

Run `scripts/ds/removed-command.mjs` against `component.nonexistent-thing`, then `npm run ds:frobnicate`.
