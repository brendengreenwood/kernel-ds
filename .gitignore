node_modules/
dist/
.netlify/
.claude/settings.local.json
.claude/skills/

# source archive — contents are extracted into the repo
kernel ds.zip

# ds-bundle — generated build artifact (node kernel-portal/scripts/build-ds-bundle.mjs)
ds-bundle/

# studio dev server logs — session artifacts
studio-dev.*.log

# CI pack-smoke tarball output (ds:pack --write --out .ci-packs)
.ci-packs/

# release impact manifests — regenerated from changesets (npm run release:impact)
.release/
