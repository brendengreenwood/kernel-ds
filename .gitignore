node_modules/
dist/
.netlify/
.claude/settings.local.json
.claude/skills/

# source archive — contents are extracted into the repo
kernel ds.zip

# ds-bundle — generated build artifact (node kernel-portal/scripts/build-ds-bundle.mjs)
ds-bundle/
