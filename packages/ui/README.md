# @kernel/ui

Kernel's ESM React component package. Import components from `@kernel/ui`, marks from `@kernel/ui/marks`, icons from `@kernel/ui/icon`, utilities from `@kernel/ui/utils`, and theme CSS from `@kernel/ui/styles.css`.

This package is private during productionization and is validated through packed-tarball consumer tests before publication.
